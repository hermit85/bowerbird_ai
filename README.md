# deplo.app

Infrastructure operator for founders and AI builders who want to ship fast.

deplo.app automates the painful parts of shipping projects:
- environment checks
- build
- git commit + push
- Vercel deploy
- error capture
- AI repair prompts

Instead of debugging CI/CD for hours, you run one command.

---

# Quickstart

Install dependencies:

```bash
npm install
